# 作業メモ（2026-09-11時点）

## 現在のタスク: クイズ難易度5段階化＋タグベース選出方式への移行

**進行中。Webサイト（本番公開サイト）にはまだ一切反映されていません。**

- GitHubにコミット・プッシュ済みなのは以下の2ファイルと、このメモ自体のみ
  - `sql/ImportLyricsDevFromSupabase.bas`
  - `sql/ImportSoundsFromSupabase.bas`
  - いずれもExcelマクロ（データ入力用ツール）であり、アプリ本体のコードではないため、これらのプッシュだけではWebサイトの見た目・動作は一切変わらない
- 肝心のアプリ本体（`src/App.jsx`, `src/App.css`）や、他のSQL関連ファイル（`sql/add_tier_tags.sql`, `sql/add_bars_per_phrase.sql`など）は、**まだこのPC上の未コミットの変更のまま**で、GitHubにも上がっていない
- `my-website-dev`には自動デプロイの仕組みがなく、コミット・プッシュしただけではWebサイトに反映されない。反映するには別途 `npm run deploy` を実行する必要があるが、**それもまだ実行していない**
- つまり、今回の5段階化の機能はコード上は実装済みだが、GitHub・Webサイトのどちらにもまだ出ていない状態。ユーザーから明示的な指示があるまで、コミット・プッシュ・デプロイは行わない

## 背景・経緯

- 旧: `quizzes.easy/normal/hard/expert`という手動重み列で難易度4段階（やさしい/ふつう/むずかしい/エキスパート）を運用
- 新: `sounds.mv/fam`, `lyrics_dev."unique"/length`という新タグ列を追加し、タグ条件で選出する5段階（やさしい/ふつう/むずかしい/げきむず/きちく）に移行
- 内部キー対応: やさしい=`easy`、ふつう=`normal`、むずかしい=`hard`（新設）、げきむず=`superhard`（旧hardの内容を引き継ぐ）、きちく=`expert`
- タグ未設定のうちはゲームが壊れないよう、タグ条件に1件もヒットしなければ旧重み列にフォールバックし、それも空なら無条件で全件対象にする設計（`resolveTierPool`）

## 現在の最終ロジック（`src/App.jsx` 460行目付近）

**2026-09-17 大きな方針転換**: `length`の意味を「小節数（bars_per_phrase基準の正規化）」から**「歌詞の文字数」**に変更した。理由: 小節数は曲のビート感・テンポで体感の長さが変わり複雑になりすぎたため、実際の全歌詞の文字数分布（`sql/char_count.txt`、後述の`CountLyricChars`関数で算出）を見て、単純な文字数のしきい値で区切る方式に戻した。`sounds.bars_per_phrase`はカラムとしては残っているが、アプリのロジックからは未使用（今後使わない）。

**2026-09-18 しきい値の再調整**: 全6791件の歌詞を一度ローマ字に変換してから同じ数え方でカウントし直したところ（`sql/char_count.txt`を更新）、分布が変わったため、げきむず/むずかしい/ふつうの境界を **げきむず=0〜5文字、むずかしい=6〜12文字、ふつう=13文字以上**（旧: 1〜5 / 6〜14 / 15以上）に変更した。カテゴリごとの割合は概ね15%/39%/46%で以前とほぼ同じ比率を維持。

```js
// lengthは歌詞の文字数（「」()（）等の記号・スペース・改行を除いた実質文字数。
// 半角アルファベットは0.5文字換算。CountLyricChars関数と同じ数え方）。
// 文字数の分布（全歌詞をローマ字変換してカウントした実データ）をもとに、
// げきむず=0〜5文字、むずかしい=6〜12文字、ふつう=13文字以上、という区切りで判定する。
const TIER_TAG_FILTER = {
  easy: (q) => q.unique === true && getPersonCount(q) === 1,
  normal: (q) => (q.mv === true || q.fam === true) && q.length != null && q.length >= 13 && getPersonCount(q) === 1 && !isRepeatedInSong(q),
  // 6〜12文字・1人または全員が対象
  hard: (q, totalMembers) => {
    if (q.length == null || q.length < 6 || q.length > 12) return false;
    const c = getPersonCount(q);
    return c === 1 || c === totalMembers;
  },
  // 5文字以下（0文字含む）・1人または全員が対象
  superhard: (q, totalMembers) => {
    if (q.length == null || q.length > 5) return false;
    const c = getPersonCount(q);
    return c === 1 || c === totalMembers;
  },
  expert: (q) => getPersonCount(q) >= 2,
};

// タグ条件がヒットしなかった場合に試す、旧重み列（優先順）
const TIER_FALLBACK_KEYS = {
  easy: ['easy'],
  normal: ['normal'],
  hard: ['normal', 'easy'],
  superhard: ['hard'],
  expert: ['expert'],
};

// 文字数の範囲自体で難易度を区切っているため、範囲内は均等。全員で歌う歌詞だけ出にくくする。
// easyは、前後の歌詞や歌詞自体に歌い手の名前が入っていて簡単すぎるものを
// lyrics_dev.weight（デフォルト1、下げたい場合は0.1〜0.5など）で出にくくする。
const TIER_TAG_WEIGHT_FN = {
  easy: (q) => (q.weight != null ? q.weight : 1),
  hard: (q, totalMembers) => (getPersonCount(q) === totalMembers ? 0.3 : 1),
  superhard: (q, totalMembers) => (getPersonCount(q) === totalMembers ? 0.1 : 1),
  expert: (q, totalMembers) => (getPersonCount(q) === totalMembers ? 0.1 : 1),
};

const isRepeatedInSong = (q) => Array.isArray(q.occurrence) && q.occurrence.some(v => v != null);
```

### tierごとの条件表（人間可読版、長さ＝文字数）

| tier | タグ条件 | 重み |
|---|---|---|
| やさしい(`easy`) | `unique=true` かつ1人 | `weight`列があればその値（デフォルト1） |
| ふつう(`normal`) | (`mv=true`または`fam=true`) かつ 13文字以上 かつ1人 かつ繰り返しフレーズ(`occurrence`設定済み)は除外 | なし（均等） |
| むずかしい(`hard`) | 6〜12文字 かつ（1人または全員） | 全員曲→0.3、それ以外→1（均等） |
| げきむず(`superhard`) | 5文字以下（0文字含む） かつ（1人または全員） | 全員曲→0.1、それ以外→1（均等） |
| きちく(`expert`) | 2人以上 | 全員曲→0.1、それ以外→1 |

- `fam`単体では使われず、normalでmv/famのOR条件としてのみ使用
- `unique`は「内容からだれが歌っているか分かりやすい歌詞」の意味で運用（description文言もそれに合わせて変更済み）
- `occurrence`（既存列、曲中で同じフレーズが複数回登場する場合に値が入る）を「繰り返しフレーズ判定」に転用。新カラムの追加はしていない
- 「」・全角（）は記号だけ除去して中身は歌詞としてカウント。半角()は合いの手表現のため中身ごと除外。`"..."`は引用符記号だけ除去（中身は残す）。`・↑↓-－/\／＼`は単独除去。全角半角スペース・改行も除去。半角アルファベットは2文字で1文字カウント
- 文字数のしきい値（1〜5 / 6〜14 / 15以上）は`sql/char_count.txt`の実データ分布（約6,791件、15%/39%/31%相当）をもとに決定。体感に応じて今後調整の可能性あり
- `weight`（2026-09-17追加）: 「やさしい」の`unique=true`歌詞のうち、前後の歌詞や歌詞自体に歌い手の名前が書かれていて簡単すぎるものだけ、出題対象から完全に外さず頻度だけ下げるための実数カラム。未入力（NULL）は1扱い。`sql/add_lyric_weight.sql`で`lyrics`/`lyrics_dev`両方に追加し、`quiz_full_dev`/`song_lyrics_dev`ビュー末尾にも追加済み（Supabaseで実行済み）

## 変更ファイル（未コミット）

- `src/App.jsx` — 上記ロジック本体、`debugDiffs`/`difficultyLabel`/`descriptions`/`resultMessages`/JSX配列などを5段階に拡張
- `src/App.css` — 5番目のdifficulty-gridボタンの中央寄せ、`superhard`用の配色追加
- `sql/add_tier_tags.sql`（新規）— `sounds.mv/fam`, `lyrics/lyrics_dev."unique"/length`カラム追加＋`quiz_full_dev`/`song_lyrics_dev`ビュー更新。**Supabaseで実行済み**（列順の衝突エラーを`IF NOT EXISTS`＋列を末尾に追加する形で修正し、最終的に成功）
- `sql/add_bars_per_phrase.sql`（新規、**Supabaseで実行済み**）— `sounds.bars_per_phrase`カラム追加＋`quiz_full_dev`ビューに`bars_per_phrase`を末尾追加
- `sql/ImportLyricsDevFromSupabase.bas` — 既存マクロを拡張し、`lyrics`シート取り込み列に`unique`・`length`・`weight`を追加
- `sql/ImportSoundsFromSupabase.bas`（新規）— `sounds`テーブルを`sounds_tags`シートへ取り込む新マクロ。`bars_per_phrase`も取り込み列に追加済み
- `sql/excel_to_sql.js` — `u-t`（lyrics_dev の unique/length/weight 更新SQL生成）・`u-st`（sounds の mv/fam/bars_per_phrase 更新SQL生成）モードを追加
- `sql/add_lyric_weight.sql`（新規、**Supabaseで実行済み**）— `lyrics`/`lyrics_dev.weight`カラム追加＋`quiz_full_dev`/`song_lyrics_dev`ビューに`weight`を末尾追加

## データ入力の進捗

- **`sounds.mv`/`fam`**: Excelの`sounds_tags`シートで入力完了。Supabaseに反映済み。
- **`lyrics_dev."unique"`/`length`**: 入力完了・Supabaseに反映済み（2026-09-17確認: `unique=true`が267件、`length`が6795/6796件で入力済み）。
  - 途中、`ImportLyricsFromSupabase`マクロを再実行した際に`lyrics`シートが丸ごと作り直され、Supabaseにまだ反映していなかった`unique`の入力が一度消える事故があった（DB側は元々NULLだったため実データの損失はなし）。`unique`は再入力して反映済み。
  - **教訓**: `unique`/`length`/`weight`をExcelで入力したら、次に`ImportLyricsFromSupabase`を再実行する前に必ずSupabaseへ反映（`u-t`→SQL実行）しておくこと。再実行するとシートがSupabaseの最新データで上書きされ、未反映の入力は失われる。
  - `CountLyricChars`関数の除外ルールは最終的に以下で確定：「」・全角（）は中身を残し記号だけ除去、半角()は合いの手のため中身ごと除外、`"..."`は引用符記号だけ除去（中身は残す）、`・↑↓-－/\／＼.…、!?！？`は単独除去、全角半角スペース・改行も除去、半角アルファベットは0.5文字換算
- **`sounds.bars_per_phrase`**: カラム追加済み（Supabase実行済み）だが、**方針転換によりアプリのロジックでは使わなくなった**。入力は不要。
- **`lyrics_dev.weight`**（2026-09-17追加、新規カラム）: 「やさしい」で、前後の歌詞や歌詞自体に歌い手の名前が書かれていて簡単すぎる歌詞だけ、出題率を下げるための値。未入力（大半）は1扱い。入力はこれから。

## ワークフロー（今後もこの手順で継続）

1. Excelブック `sql/sqlコンバーター_.xlsm` で `ImportLyricsDevFromSupabase` / `ImportSoundsFromSupabase` マクロを実行してデータ取得
2. Excel上で `unique`/`length`（`lyrics`シート）や `mv`/`fam`（`sounds_tags`シート）を入力し、保存
   - `length`（文字数）は手入力ではなく、`lyrics`シートに`CountLyricChars`関数（後述）で自動算出する
3. `sql`フォルダで `node excel_to_sql.js u-t`（歌詞タグ）または `node excel_to_sql.js u-st`（曲タグ）を実行
4. 生成された `sql/sqlコンバーター__update_lyric_tags.sql` / `sql/sqlコンバーター__update_sounds_tags.sql` をSupabase SQL Editorで実行
5. `id`列で直接UPDATEするため、group_name/song_name/seqの突合ミスは発生しない

※ `sql`フォルダの`node_modules`に`xlsx`パッケージをインストール済み（`npm install`実行済み。package.json/package-lock.jsonへの追記あり）。

### 歌詞文字数の自動カウント（`CountLyricChars`関数）

`lyrics`シートに標準モジュールとして以下を貼り付けて使う（ブック内にVBAコードとして保存済みではなく、ユーザーがExcel側で手動貼付・保存する運用。リポジトリのファイルとしては管理していない）。

- `CountLyricChars(s)`: 「」・全角（）は記号だけ除去して中身は歌詞としてカウント。半角()は合いの手表現のため中身ごと除外。`"..."`は引用符記号だけ除去（中身は残す）。`・↑↓-－/\／＼`は単独除去。全角半角スペース・改行も除去。半角アルファベットは2文字で1文字カウント
- `FillCharCountColumn`: `lyrics`シートのF列（`lyric`）を最終行まで読み、M列に`char_count`見出し＋`=CountLyricChars(F2)`を自動入力するマクロ

`u-t`（`generateUpdateLyricTags`）は`length`列ではなく、このM列（`char_count`）の値を読んで`lyrics_dev.length`に反映するよう対応済み（`length`列への手動コピーは不要）。半角アルファベット0.5文字換算で小数になる場合は四捨五入してから反映する。

### `excel_to_sql.js`のバグ修正（2026-09-17）

`u-l`・`u-dl`・`u-lo`・`u-a`・`u-m`が、`lyrics_dev`ではなく**本番の`lyrics`テーブル**を参照・更新するようになっていた（`lyrics_dev`導入前の古いコードのまま放置されていた）。全モードを`lyrics_dev`参照に修正済み。また、撤廃済みのヒント機能専用だった`u-nh`（`needs_hint`更新）モードは削除した。

`ImportLyricsDevFromSupabase.bas`のVLOOKUP参照先も`Sounds`（存在しないシート）から`sounds_tags`に修正し、さらに`VALUE(B2)`が型不一致（`sounds_id`も`id`もPower Query経由でテキスト型）で`#N/A`になっていたため`VALUE()`を除去して修正済み。

### インポートマクロのページング結合バグ（2026-09-18）

`ImportLyricsDevFromSupabase.bas`（および同じ構造の`ImportQuizFullDevFromSupabase.bas`/`ImportSoundsFromSupabase.bas`）で、Supabaseから複数ページ（`PAGE_SIZE=1000`）取得したCSVを単純連結していたところ、**各ページのレスポンス末尾には改行が付いていない**ことが判明（curlで実データ確認済み）。そのため「前ページの最終行」と「次ページの1行目（ヘッダー除去後）」が改行なしで融合し、1行に混ざってしまっていた。

症状: Excelにインポートすると、一部の行の`weight`列に次の行の`id`のような数値が入り込み、その次の行がシートから消えて見える（実際にはSupabase側のデータは無事で、Excelへの取り込み時に1行に融合していただけ）。PAGE_SIZE=1000・全約6791件なのでページの継ぎ目は6箇所程度、その全てで発生しうる。

修正: 2ページ目以降を書き込む直前に区切りの改行（LFバイト）を1つ追加するよう3つのマクロを修正済み。**Excel側のVBAプロジェクトは自動更新されないため、修正後のマクロ本文を再度貼り付けてから再実行する必要がある。**

## 次にやること

- `lyrics`シートの`weight`列に、前後の歌詞や歌詞自体で歌い手が分かってしまう「やさしい」歌詞の出題率を下げる値（0.1〜0.5程度）を入力していく
- `node excel_to_sql.js u-t`でSQL生成（`weight`列も反映されるようになった）→Supabaseで実行
- `sounds.bars_per_phrase`は今回の方針転換でアプリロジックからは不要になったため、入力しなくてよい
- 実際にアプリ（`npm run dev`）で各難易度の出題内容を確認する
- コミット・プッシュ・デプロイは、ユーザーから明示的な指示があるまで行わない
