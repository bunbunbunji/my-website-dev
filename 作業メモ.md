# 作業メモ（2026-09-11時点）

## 現在のタスク: クイズ難易度5段階化＋タグベース選出方式への移行

**進行中。まだコミット・プッシュしていません（ユーザーの明示指示があるまでしない）。**
すべてローカルの未コミット変更（`src/App.jsx`, `src/App.css`, `sql/`配下）のまま。

## 背景・経緯

- 旧: `quizzes.easy/normal/hard/expert`という手動重み列で難易度4段階（やさしい/ふつう/むずかしい/エキスパート）を運用
- 新: `sounds.mv/fam`, `lyrics_dev."unique"/length`という新タグ列を追加し、タグ条件で選出する5段階（やさしい/ふつう/むずかしい/げきむず/きちく）に移行
- 内部キー対応: やさしい=`easy`、ふつう=`normal`、むずかしい=`hard`（新設）、げきむず=`superhard`（旧hardの内容を引き継ぐ）、きちく=`expert`
- タグ未設定のうちはゲームが壊れないよう、タグ条件に1件もヒットしなければ旧重み列にフォールバックし、それも空なら無条件で全件対象にする設計（`resolveTierPool`）

## 現在の最終ロジック（`src/App.jsx` 420〜495行目付近）

```js
// 曲によって「1フレーズの小節数」が違う（曲調・テンポ感で変わる）ため、
// lengthは実際の小節数をそのまま入力してもらい、曲ごとのbars_per_phraseを基準に
// 正規化（基準4小節換算）してから判定する。未設定の曲は基準4小節（従来通り）として扱う。
const getBasisBars = (q) => q.bars_per_phrase || 4;
const getNormalizedLength = (q) => (q.length == null ? null : q.length * (4 / getBasisBars(q)));

const TIER_TAG_FILTER = {
  easy: (q) => q.unique === true && getPersonCount(q) === 1,
  normal: (q) => (q.mv === true || q.fam === true) && q.length != null && getNormalizedLength(q) >= 2 && getPersonCount(q) === 1 && !isRepeatedInSong(q),
  // 正規化後の長さ1以上すべてが対象（2以上は重みで出にくくするだけで、プールからは除外しない）
  hard: (q) => q.length != null && getNormalizedLength(q) >= 1 && getPersonCount(q) === 1,
  // 長さ制限なし（値さえ設定されていれば対象。長さで出やすさに強弱をつけるのは重み側）
  superhard: (q, totalMembers) => {
    if (q.length == null) return false;
    const c = getPersonCount(q);
    return c === 1 || c === totalMembers;
  },
  expert: (q) => getPersonCount(q) >= 2,
};

// タグ条件がヒットしなかった場合に試す、旧重み列（優先順）
const TIER_FALLBACK_KEYS = {
  easy: ['easy'],
  normal: ['normal'],
  hard: ['normal', 'easy'],
  superhard: ['hard'],
  expert: ['expert'],
};

// タグ一致プール内で出やすさに強弱をつけたいtierのみ定義。未定義のtierは完全ランダム。
const TIER_TAG_WEIGHT_FN = {
  hard: (q) => (Math.round(getNormalizedLength(q)) === 1 ? 1.0 : 0.3),
  superhard: (q, totalMembers) => {
    const isAll = getPersonCount(q) === totalMembers;
    const nlen = Math.round(getNormalizedLength(q));
    if (nlen === 0) return isAll ? 0.1 : 1.0;
    if (nlen === 1) return isAll ? 0.1 : 0.3;
    return 0.01;
  },
  expert: (q, totalMembers) => (getPersonCount(q) === totalMembers ? 0.1 : 1),
};

const isRepeatedInSong = (q) => Array.isArray(q.occurrence) && q.occurrence.some(v => v != null);
```

### tierごとの条件表（人間可読版、長さは正規化後＝16ビート換算の値）

| tier | タグ条件 | 重み |
|---|---|---|
| やさしい(`easy`) | `unique=true` かつ1人 | なし（均等） |
| ふつう(`normal`) | (`mv=true`または`fam=true`) かつ 正規化後の長さ2以上 かつ1人 かつ繰り返しフレーズ(`occurrence`設定済み)は除外 | なし（均等） |
| むずかしい(`hard`) | 正規化後の長さ1以上（上限なし）かつ1人 | 長さ=1→1.0、長さ≥2→0.3 |
| げきむず(`superhard`) | 長さ設定済み（0以上、上限なし）かつ（1人または全員） | 長さ0:1人=1.0/全員=0.1、長さ1:1人=0.3/全員=0.1、長さ2以上:1人・全員とも0.01（すべて正規化後の値） |
| きちく(`expert`) | 2人以上 | 全員曲→0.1、それ以外→1 |

- `fam`単体では使われず、normalでmv/famのOR条件としてのみ使用
- `unique`は「内容からだれが歌っているか分かりやすい歌詞」の意味で運用（description文言もそれに合わせて変更済み）
- `occurrence`（既存列、曲中で同じフレーズが複数回登場する場合に値が入る）を「繰り返しフレーズ判定」に転用。新カラムの追加はしていない
- 「げきむず・きちく以外は重み設定不要」「むずかしい・げきむずは長さで出にくさに強弱、きちくは全員曲だけ出にくく」という方針で確定
- **`length`は「実際の小節数」をそのまま入力する**（0/1/2〜3/4以上のようなカテゴリ値ではない）。曲ごとに1フレーズにあたる小節数が異なる（8ビート/16ビートなどの曲調・テンポ感で変わる）ため、8ビート/16ビートで分類するのではなく、`sounds.bars_per_phrase`（その曲の1フレーズの実際の小節数）を直接タグとして持たせ、アプリ側が`length × (4 ÷ bars_per_phrase)`で正規化してから判定する。未設定の曲は基準4小節（従来通り）として扱う

## 変更ファイル（未コミット）

- `src/App.jsx` — 上記ロジック本体、`debugDiffs`/`difficultyLabel`/`descriptions`/`resultMessages`/JSX配列などを5段階に拡張
- `src/App.css` — 5番目のdifficulty-gridボタンの中央寄せ、`superhard`用の配色追加
- `sql/add_tier_tags.sql`（新規）— `sounds.mv/fam`, `lyrics/lyrics_dev."unique"/length`カラム追加＋`quiz_full_dev`/`song_lyrics_dev`ビュー更新。**Supabaseで実行済み**（列順の衝突エラーを`IF NOT EXISTS`＋列を末尾に追加する形で修正し、最終的に成功）
- `sql/add_bars_per_phrase.sql`（新規、**Supabaseで実行済み**）— `sounds.bars_per_phrase`カラム追加＋`quiz_full_dev`ビューに`bars_per_phrase`を末尾追加
- `sql/ImportLyricsDevFromSupabase.bas` — 既存マクロを拡張し、`lyrics`シート取り込み列に`unique`・`length`を追加
- `sql/ImportSoundsFromSupabase.bas`（新規）— `sounds`テーブルを`sounds_tags`シートへ取り込む新マクロ。`bars_per_phrase`も取り込み列に追加済み
- `sql/excel_to_sql.js` — `u-t`（lyrics_dev の unique/length 更新SQL生成）・`u-st`（sounds の mv/fam/bars_per_phrase 更新SQL生成）モードを追加

## データ入力の進捗

- **`sounds.mv`/`fam`**: Excelの`sounds_tags`シートで入力完了（ユーザーより「soundsが完成しました」と報告あり）。`node excel_to_sql.js u-st`で`sql/sqlコンバーター__update_sounds_tags.sql`（145行）を生成済み。**Supabaseでの実行有無は要確認**（このSQLをまだ実行していなければ実行が必要）
- **`lyrics_dev."unique"`**: Excelの`lyrics`シートで入力完了（ユーザーより「uniqueだけ埋めました」と報告あり）。`node excel_to_sql.js u-t`で`sql/sqlコンバーター__update_lyric_tags.sql`（6863行）を生成済み。**Supabaseでの実行有無は要確認**
- **`lyrics_dev.length`**: 未入力（全行NULL）。今後Excelの`lyrics`シートで入力予定。**入力方式が変更になった**: カテゴリ値ではなく実際の小節数を入力する（下記参照）
- **`sounds.bars_per_phrase`**: カラム追加済み（Supabase実行済み）。`ImportSoundsFromSupabase`マクロ再実行、`sounds_tags`シートへの入力はこれから

## ワークフロー（今後もこの手順で継続）

1. Excelブック `sql/sqlコンバーター_.xlsm` で `ImportLyricsDevFromSupabase` / `ImportSoundsFromSupabase` マクロを実行してデータ取得
2. Excel上で `unique`/`length`（`lyrics`シート）や `mv`/`fam`（`sounds_tags`シート）を入力し、保存
3. `sql`フォルダで `node excel_to_sql.js u-t`（歌詞タグ）または `node excel_to_sql.js u-st`（曲タグ）を実行
4. 生成された `sql/sqlコンバーター__update_lyric_tags.sql` / `sql/sqlコンバーター__update_sounds_tags.sql` をSupabase SQL Editorで実行
5. `id`列で直接UPDATEするため、group_name/song_name/seqの突合ミスは発生しない

※ `sql`フォルダの`node_modules`に`xlsx`パッケージをインストール済み（`npm install`実行済み。package.json/package-lock.jsonへの追記あり）。

## 次にやること

- `ImportSoundsFromSupabase`マクロを再実行して`sounds_tags`シートに`bars_per_phrase`列を取り込み、曲ごとに入力（`sql/add_bars_per_phrase.sql`は実行済み）
- `length`列の入力を進める（Excelの`lyrics`シートで。**実際の小節数**をそのまま入力する）
- 入力が進むごとに`u-t`/`u-st`でSQL再生成→Supabaseで実行、を繰り返す
- タグ入力が十分進んだら、実際にアプリ（`npm run dev`）で各難易度の出題内容を確認する
- コミット・プッシュ・デプロイは、ユーザーから明示的な指示があるまで行わない
